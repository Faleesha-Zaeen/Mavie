{
  "name": "mavie",
  "version": "0.1.0",
  "private": true,
  "description": "MAVIE — AI beauty and fashion decision companion. A look made for your life.",
  "scripts": {
    "setup": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c magenta,cyan \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "check:youcam": "npm run check:youcam --prefix server",
    "find:youcam": "npm run find:youcam --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server",
    "preflight": "npm run preflight --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
